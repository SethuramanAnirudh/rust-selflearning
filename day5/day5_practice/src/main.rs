fn main() {
    let mut x = 5; // mut makes the variable mutable
                   // by default variables are immutable
    println!("The value of x is {x}");
    x = 6;
    println!("The value of x is {x}");
}
